<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Auré Atelier - Signature Cotton Collection</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Noto Sans KR', sans-serif;
            background-color: #0E0E0E;
            color: #F7F5EF;
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* Header Styles */
        header {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(14, 14, 14, 0.95);
            backdrop-filter: blur(10px);
            z-index: 1000;
            padding: 1rem 0;
            border-bottom: 1px solid rgba(192, 162, 109, 0.2);
        }

        .header-container {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 2rem;
        }

        .logo {
            font-family: Georgia, Times, serif;
            font-size: 1.8rem;
            font-weight: 300;
            letter-spacing: 2px;
            color: #C0A26D;
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 2.5rem;
        }

        nav a {
            color: #F7F5EF;
            text-decoration: none;
            font-weight: 300;
            letter-spacing: 0.5px;
            transition: color 0.3s ease;
            cursor: pointer;
        }

        nav a:hover {
            color: #C0A26D;
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 6rem 2rem 4rem;
            background: linear-gradient(135deg, rgba(192, 162, 109, 0.1) 0%, rgba(14, 14, 14, 1) 70%);
        }

        .hero h1 {
            font-family: Georgia, Times, serif;
            font-size: clamp(2.5rem, 6vw, 4rem);
            font-weight: 300;
            letter-spacing: 3px;
            margin-bottom: 1.5rem;
            color: #C0A26D;
        }

        .hero p {
            font-size: 1.2rem;
            color: #B6B1A9;
            max-width: 600px;
            margin-bottom: 3rem;
            line-height: 1.8;
        }

        .cta-buttons {
            display: flex;
            gap: 1.5rem;
            flex-wrap: wrap;
            justify-content: center;
        }

        .btn {
            padding: 1rem 2.5rem;
            border: 2px solid #C0A26D;
            background: transparent;
            color: #C0A26D;
            text-decoration: none;
            border-radius: 16px;
            font-weight: 500;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            cursor: pointer;
            display: inline-block;
            box-shadow: 0 4px 20px rgba(192, 162, 109, 0.2);
        }

        .btn:hover {
            background: #C0A26D;
            color: #0E0E0E;
            transform: translateY(-2px);
            box-shadow: 0 8px 30px rgba(192, 162, 109, 0.3);
        }

        .btn-primary {
            background: #C0A26D;
            color: #0E0E0E;
        }

        .btn-primary:hover {
            background: transparent;
            color: #C0A26D;
        }

        /* Section Styles */
        section {
            padding: 5rem 2rem;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .section-title {
            font-family: Georgia, Times, serif;
            font-size: clamp(2rem, 4vw, 2.5rem);
            font-weight: 300;
            text-align: center;
            margin-bottom: 3rem;
            color: #C0A26D;
            letter-spacing: 2px;
        }

        /* Card Styles */
        .cards-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .card {
            background: rgba(247, 245, 239, 0.05);
            border-radius: 16px;
            padding: 2rem;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(192, 162, 109, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 40px rgba(192, 162, 109, 0.15);
        }

        .product-image {
            width: 100%;
            height: 200px;
            background: linear-gradient(135deg, #C0A26D 0%, #B6B1A9 100%);
            border-radius: 12px;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1rem;
            color: #0E0E0E;
            font-weight: 600;
        }

        .card h3 {
            font-family: Georgia, Times, serif;
            font-size: 1.5rem;
            color: #C0A26D;
            margin-bottom: 1rem;
            letter-spacing: 1px;
        }

        .card p {
            color: #B6B1A9;
            margin-bottom: 1.5rem;
            line-height: 1.7;
        }

        .product-details {
            background: rgba(192, 162, 109, 0.1);
            padding: 1rem;
            border-radius: 8px;
            margin-bottom: 1.5rem;
        }

        .product-details h4 {
            color: #C0A26D;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
            font-weight: 600;
        }

        .product-details p {
            font-size: 0.85rem;
            color: #F7F5EF;
            margin-bottom: 0.5rem;
        }

        /* Set Section Specific */
        .set-cards {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 2rem;
            margin-top: 3rem;
        }

        .set-main-card {
            grid-column: 1;
        }

        .info-card {
            grid-column: 2;
            display: flex;
            flex-direction: column;
            justify-content: space-around;
        }

        .info-icons {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .info-item {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .info-icon {
            width: 40px;
            height: 40px;
            background: #C0A26D;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            color: #0E0E0E;
        }

        /* Apply Section */
        .apply-section {
            background: linear-gradient(135deg, rgba(192, 162, 109, 0.1) 0%, rgba(14, 14, 14, 1) 100%);
            text-align: center;
        }

        .apply-content {
            max-width: 600px;
            margin: 0 auto;
        }

        .apply-link {
            display: inline-block;
            padding: 1.5rem 3rem;
            background: linear-gradient(135deg, #C0A26D 0%, #B6B1A9 100%);
            color: #0E0E0E;
            text-decoration: none;
            border-radius: 16px;
            font-size: 1.2rem;
            font-weight: 600;
            letter-spacing: 1px;
            margin: 2rem 0;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            box-shadow: 0 8px 30px rgba(192, 162, 109, 0.3);
        }

        .apply-link:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 40px rgba(192, 162, 109, 0.4);
        }

        .apply-note {
            font-size: 0.9rem;
            color: #B6B1A9;
            font-style: italic;
        }

        /* Footer */
        footer {
            background: rgba(247, 245, 239, 0.05);
            padding: 3rem 2rem 2rem;
            border-top: 1px solid rgba(192, 162, 109, 0.2);
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            text-align: center;
        }

        .footer-info {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-bottom: 2rem;
        }

        .footer-info p {
            color: #B6B1A9;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }

        .copyright {
            border-top: 1px solid rgba(192, 162, 109, 0.2);
            padding-top: 1.5rem;
            color: #B6B1A9;
            font-size: 0.8rem;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .header-container {
                padding: 0 1rem;
            }

            nav ul {
                gap: 1.5rem;
            }

            nav a {
                font-size: 0.9rem;
            }

            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }

            .cards-grid {
                grid-template-columns: 1fr;
            }

            .set-cards {
                grid-template-columns: 1fr;
            }

            .set-main-card {
                grid-column: 1;
            }

            .info-card {
                grid-column: 1;
            }

            .info-icons {
                flex-direction: row;
                justify-content: space-around;
                flex-wrap: wrap;
            }

            .footer-info {
                grid-template-columns: 1fr;
                text-align: left;
            }
        }

        /* Smooth animations */
        .card, .btn, .apply-link {
            animation: fadeInUp 0.6s ease-out;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Scroll behavior */
        html {
            scroll-behavior: smooth;
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <div class="header-container">
            <div class="logo">Haneul Worship</div>
            <nav>
                <ul>
                    <li><a href="#apply-section">신청</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <h1>Premium Cotton Collection</h1>
        <p>엄선된 코튼으로 제작한 시그니처 하늘 예배팀 티셔츠와 프리미엄 집업 후드.<br>
        세트로 구매하시거나 개별 선택이 가능합니다.</p>
        <div class="cta-buttons">
            <a href="#set-section" class="btn">세트 보기</a>
            <a href="#apply-section" class="btn btn-primary">신청하기</a>
        </div>
    </section>

    <!-- Set Section -->
    <section id="set-section">
        <div class="container">
            <h2 class="section-title">Complete Set</h2>
            <div class="set-cards">
                <div class="card set-main-card">
                    <div class="product-image">SET COLLECTION</div>
                    <h3>Signature Cotton Set</h3>
                    <p>프리미엄 티셔츠와 집업 후드를 함께 구성한 완벽한 세트입니다. 
                    일상에서 편안하게 착용할 수 있는 고품질 소재로 제작되었습니다.</p>
                    
                    <div class="product-details">
                        <h4>세트 구성</h4>
                        <p>• 시그니처 반팔 티셔츠 × 1</p>
                        <p>• 프리미엄 후드집업 × 1</p>
                    </div>
                    
                    <div class="product-details">
                        <h4>소재 & 관리법</h4>
                        <p>• 프리미엄 사랑을 넣습니다.</p>
                        <p>• 함께 입으면 더욱 좋습니다.</p>
                        <p>• 온 마음을 다해 섬기는 예배팀 화이팅</p>
                    </div>
                    
                    <a href="#apply-section" class="btn btn-primary">신청하기</a>
                </div>

                <div class="card info-card">
                    <div class="info-icons">
                        <div class="info-item">
                            <div class="info-icon">🚚</div>
                            <div>
                                <h4>무료배송</h4>
                                <p>매주 주일 받으실 수 있습니다.<br>일주일 소요</p>
                            </div>
                        </div>
                        <div class="info-item">
                            <div class="info-icon">↩️</div>
                            <div>
                                <h4>교환불가</h4>
                                <p>입으시면 끝~<br>ㅎㅎ</p>
                            </div>
                        </div>
                        <div class="info-item">
                            <div class="info-icon">🏆</div>
                            <div>
                                <h4>품질보증</h4>
                                <p>프리미엄 품질<br>A/S 가능</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Individual Section -->
    <section id="individual-section">
        <div class="container">
            <h2 class="section-title">Individual Items</h2>
            <div class="cards-grid">
                <div class="card">
                    <div class="product-image">COTTON TEE</div>
                    <h3>Signature Cotton Tee</h3>
                    <p>부드럽고 편안한 착용감의 프리미엄 반팔 티셔츠입니다. 
                    심플한 디자인으로 어떤 스타일에도 잘 어울립니다.</p>
                    
                    <div class="product-details">
                        <h4>소재 & 관리법</h4>
                        <p>• 모든 것을 주께하듯 관리해주세요</p>
                        <p>• 사랑으로 안아주세요</p>
                        <p>• 이 티셔츠와 함께 우리는 하나</p>
                    </div>
                    
                    <a href="#apply-section" class="btn">신청</a>
                </div>

                <div class="card">
                    <div class="product-image">ZIP HOODIE</div>
                    <h3>Premium Zip Hoodie</h3>
                    <p>고급스러운 질감의 집업 후드입니다. 
                    적당한 두께로 사계절 착용 가능하며 활동성이 뛰어납니다.</p>
                    
                    <div class="product-details">
                        <h4>소재 & 관리법</h4>
                        <p>• 하나님과 함께하면 더욱 좋답니다.</p>
                        <p>• 예배섬김때 착용 권장합니다.</p>
                        <p>• 사계절 내내 입으면 더 좋아요~</p>
                    </div>
                    
                    <a href="#apply-section" class="btn">신청</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Apply Section -->
    <section id="apply-section" class="apply-section">
        <div class="container">
            <div class="apply-content">
                <h2 class="section-title">Order Application</h2>
                <p>원하시는 제품을 선택하셨나요? 아래 링크를 통해 주문을 진행해 주세요. 
                사이즈, 색상, 수량 등 자세한 옵션을 선택하실 수 있습니다.</p>
                
                <a href="https://example.com/apply" class="apply-link" target="_blank">
                    신청 링크
                </a>
                
                <p class="apply-note">
                    * 위 링크를 실제 신청폼 링크로 교체하시면 완료됩니다.
                </p>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="footer-content">
            <div class="footer-info">
                <div>
                    <p><strong>상호:</strong> Haneul Worship Team</p>
                    <p><strong>대표자:</strong> Jesus</p>
                    <p><strong>사업자등록번호:</strong> I'm nothing. Without Love.</p>
                </div>
                <div>
                    <p><strong>주소:</strong> 우리가 꿈꾸는 나라</p>
                    <p><strong>전화:</strong> 기도</p>
                    <p><strong>이메일:</strong> 찬양</p>
                </div>
            </div>
            <div class="copyright">
                <p>&copy; <span id="current-year"></span> Haneul Worship Team. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script>
        // Smooth scroll navigation
        document.querySelectorAll('nav a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    const headerHeight = document.querySelector('header').offsetHeight;
                    const targetPosition = target.offsetTop - headerHeight - 20;
                    
                    window.scrollTo({
                        top: targetPosition,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // CTA button smooth scroll
        document.querySelectorAll('.btn[href^="#"]').forEach(button => {
            button.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    const headerHeight = document.querySelector('header').offsetHeight;
                    const targetPosition = target.offsetTop - headerHeight - 20;
                    
                    window.scrollTo({
                        top: targetPosition,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Auto-update copyright year
        document.getElementById('current-year').textContent = new Date().getFullYear();

        // Add scroll effect to header
        window.addEventListener('scroll', function() {
            const header = document.querySelector('header');
            if (window.scrollY > 100) {
                header.style.background = 'rgba(14, 14, 14, 0.98)';
            } else {
                header.style.background = 'rgba(14, 14, 14, 0.95)';
            }
        });
    </script>
</body>
</html>
